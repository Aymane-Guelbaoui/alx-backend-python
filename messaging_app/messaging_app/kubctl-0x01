#!/bin/bash
# Scale Django app to 3 replicas and verify

echo "⚡ Scaling deployment..."
kubectl scale deployment messaging-app --replicas=3

echo "📦 Checking running pods..."
kubectl get pods

echo "📊 Monitoring resource usage..."
kubectl top pods

echo "🔥 Running load test with wrk (make sure wrk is installed)..."
wrk -t4 -c100 -d30s http://$(minikube ip):8000/
